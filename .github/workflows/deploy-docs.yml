name: Call Deploy Docs

on:
  push:
    branches:
      - main  # Or adjust as needed for your workflow

jobs:
  call-deploy:
    uses: Telucero/workflows/deploy-docs.yml@Central_repo_test  
    with:
      branch: main  # Branch to deploy from in the docs repo
      repository: Telucero/moonbeam-docs  
      path: moonbeam-docs  
      url_prefix: moonbeam-mkdocs  
      submodules: true  # Optional, set to false if not using submodules
    secrets: inherit  
